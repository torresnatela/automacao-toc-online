node_modules/
.turbo/
dist/
.next/
out/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
supabase/.branches/
supabase/.temp/
playwright-report/
test-results/
# storageState do Playwright: cookies de sessão do TOConline são equivalentes a
# credencial. Nunca versionar.
.rpa/

# Artefactos do reconhecimento da AT (screenshots do portal com dados reais de
# clientes, redes, formulários). Partilha só pelo canal seguro do gabinete.
apps/worker/recon/

# Rascunho do Playwright MCP: snapshots de páginas com dados reais de clientes. Nunca versionar.
.playwright-mcp/
