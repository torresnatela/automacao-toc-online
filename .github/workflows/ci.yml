name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      # Testes de integração do @toc/db exigem Supabase local (RLS depende de
      # auth.users/auth.uid()); no CI eles são pulados. Rodam localmente.
      SKIP_DB_TESTS: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm typecheck

      - name: Unit tests (@toc/core)
        run: pnpm --filter @toc/core test

      - name: DB tests (@toc/db, pulados no CI)
        run: pnpm --filter @toc/db test

      # O worker precisa de chromium para os testes de fixture local, que são
      # os que provam a travessia de shadow DOM e a janela de estabilidade da
      # leitura do grid. Não tocam na rede externa nem no TOConline.
      - name: Install chromium (Playwright)
        run: pnpm --filter @toc/worker exec playwright install --with-deps chromium

      - name: Worker tests (@toc/worker)
        run: pnpm --filter @toc/worker test

      - name: Build web
        run: pnpm --filter web build
        env:
          NEXT_PUBLIC_SUPABASE_URL: http://localhost:54421
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-dummy-anon-key
